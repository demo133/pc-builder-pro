---
description: UI设计风格规范，所有前端页面和组件必须遵循
alwaysApply: true
globs:
  - "**/*.tsx"
  - "src/app/globals.css"
---

# UI 设计风格规范

## 色彩系统

### 背景
- 页面背景：从 `#0a0e1a` 到 `#111827` 的线性渐变（从上到下）
- 卡片背景：`#1e293b`（slate-800）
- 悬浮/高亮背景：`#334155`（slate-700）

### 主色调
- 主色（青色）：`#06b6d4`（cyan-500）
- 主色悬浮：`#0891b2`（cyan-600）
- 主色浅：`#22d3ee`（cyan-400）
- 辅助色（紫色）：`#8b5cf6`（violet-500）

### 语义色
- 成功/最低价：`#10b981`（emerald-500）
- 警告：`#f59e0b`（amber-500）
- 错误/致命：`#ef4444`（red-500）
- 信息：`#3b82f6`（blue-500）

### 文字
- 主文字：`#f1f5f9`（slate-100）
- 次文字：`#94a3b8`（slate-400）
- 禁用/占位：`#64748b`（slate-500）

### 边框
- 默认边框：`#334155`（slate-700）
- 悬浮边框：`#06b6d4`（cyan-500，半透明）

## 排版
- 标题：font-bold，大号用 text-3xl/4xl
- 正文：text-sm / text-base，leading-relaxed
- 价格数字：font-mono，font-semibold
- 统一用 Inter 字体（英文）+ 系统默认中文字体

## 组件样式

### 卡片（Card）
```tsx
<Card className="bg-slate-800/50 border-slate-700 rounded-xl hover:border-cyan-500/50 transition-colors">
```
- 圆角：rounded-xl（12px）
- 背景带透明度：bg-slate-800/50
- hover 时边框变青色

### 按钮
- 主按钮：`bg-cyan-500 hover:bg-cyan-600 text-white`
- 次按钮：`bg-slate-700 hover:bg-slate-600 text-slate-100`
- 危险按钮：`bg-red-500/10 hover:bg-red-500/20 text-red-400 border border-red-500/30`
- 所有按钮：rounded-lg，transition-colors，font-medium

### 价格展示
- 当前价：text-cyan-400 font-mono font-bold
- 原价/划线价：text-slate-500 line-through text-sm
- 最低价标签：bg-emerald-500/10 text-emerald-400 text-xs px-2 py-0.5 rounded

### 兼容性标签
- fatal：`bg-red-500/10 text-red-400 border-red-500/30`
- warning：`bg-amber-500/10 text-amber-400 border-amber-500/30`
- info：`bg-blue-500/10 text-blue-400 border-blue-500/30`
- success：`bg-emerald-500/10 text-emerald-400 border-emerald-500/30`

## 布局规范

### 装机页（/builder）
- 桌面端：左右布局，左侧硬件选择区占 2/3，右侧体检报告占 1/3
- 平板/手机：上下布局，体检报告在硬件选择区下方
- 顶部固定总价栏：sticky top-0，z-50，毛玻璃效果 backdrop-blur

### 卡片间距
- 卡片之间：gap-4 或 space-y-4
- 卡片内边距：p-4 或 p-6
- 页面左右边距：max-w-7xl mx-auto px-4

## 图表样式（Recharts）
- 价格曲线：stroke="#06b6d4"，strokeWidth=2，dot=false
- 预算饼图：青色系渐变配色
- 网格线：stroke="#334155"，strokeDasharray="3 3"
- 坐标轴文字：fill="#94a3b8"，fontSize=12
- Tooltip：深色背景 bg-slate-800，边框 border-slate-600

## 交互细节
- 所有可点击元素有 cursor-pointer
- hover 有明确视觉反馈（变色/边框/阴影）
- 加载状态用 Skeleton 组件，不要用"加载中..."文字
- 空状态用图标+说明文字+操作按钮
- 过渡动画：transition-colors duration-200，不要用太花哨的动画

## 背景装饰
- 页面可加微妙的网格纹理（background-image: linear-grid）
- Hero 区域可加径向渐变光晕（radial-gradient cyan）
- 不要用大面积纯色，要有层次感

## 禁止
- 不要用浅色主题
- 不要用花哨的渐变文字
- 不要用超过3种主色调
- 不要用 emojis 代替图标（用 lucide-react）
- 不要忽略响应式，所有页面必须在手机上可用
