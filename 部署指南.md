# PC Builder Pro 部署上线指南

> 本文档面向零基础用户，手把手教你把网站发布到互联网上。

---

## 目录

1. [部署前准备](#1-部署前准备)
2. [方案一：Vercel 部署（推荐）](#2-方案一vercel-部署推荐)
3. [方案二：Railway 部署（备选）](#3-方案二railway-部署备选)
4. [环境变量配置](#4-环境变量配置)
5. [域名绑定](#5-域名绑定)
6. [部署后验证](#6-部署后验证)
7. [常见问题](#7-常见问题)

---

## 1. 部署前准备

### 1.1 注册账号

你需要注册以下账号（都免费）：

| 平台 | 用途 | 注册地址 |
|------|------|----------|
| GitHub | 代码托管 | https://github.com |
| Vercel | 网站部署（方案一） | https://vercel.com |
| Railway | 网站部署（方案二） | https://railway.app |

### 1.2 安装 Git

下载安装：https://git-scm.com/download/win

安装时全部默认下一步即可。安装完成后，在终端输入 `git --version`，能看到版本号就说明成功了。

### 1.3 上传代码到 GitHub

**第1步：在 GitHub 创建仓库**

1. 登录 GitHub，点右上角 `+` → `New repository`
2. Repository name 填：`pc-builder-pro`
3. 选 `Public`（公开，免费）
4. **不要**勾选 "Add a README file"
5. 点 `Create repository`

**第2步：本地代码推送到 GitHub**

在项目根目录 `C:\Users\GH\Doubao\chats\2026-09-05\new-chat\pc-builder` 打开终端，依次执行：

```bash
# 初始化Git仓库
git init

# 配置用户信息（替换成你的GitHub用户名和邮箱）
git config user.name "你的GitHub用户名"
git config user.email "你的GitHub邮箱"

# 添加所有文件
git add .

# 提交
git commit -m "初始提交：PC Builder Pro"

# 关联远程仓库（替换成你的仓库地址）
git branch -M main
git remote add origin https://github.com/你的用户名/pc-builder-pro.git

# 推送
git push -u origin main
```

推送时会要求输入 GitHub 用户名和密码。**注意**：GitHub 已不支持密码登录，需要用 Personal Access Token：

1. 打开 https://github.com/settings/tokens
2. 点 `Generate new token` → `Generate new token (classic)`
3. Note 填 `pc-builder`，Expiration 选 `No expiration`
4. 勾选 `repo` 权限
5. 点 `Generate token`，复制生成的 token（只显示一次！）
6. 推送时密码处粘贴这个 token

---

## 2. 方案一：Vercel 部署（推荐）

Vercel 是 Next.js 官方推荐的部署平台，免费额度足够个人使用，自动 HTTPS、自动部署。

### ⚠️ 重要：SQLite 兼容性问题

Vercel 的服务器文件系统是**只读**的，SQLite 数据库无法写入。所以部署到 Vercel 必须**更换为 PostgreSQL 数据库**。

### 2.1 创建 Vercel Postgres 数据库

1. 登录 Vercel，进入 Dashboard
2. 点 `Storage` → `Create Database`
3. 选 `Postgres`，点 `Continue`
4. 数据库名填 `pc-builder-db`，区域选 `Washington D.C. (iad1)`（离国内近）
5. 点 `Create`

创建完成后，在数据库详情页找到 `.env.local` 标签，复制里面的内容，后面要用。

### 2.2 修改项目数据库配置

**第1步：修改 schema.prisma**

打开 `prisma/schema.prisma`，把 datasource 部分改成：

```prisma
datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_PRISMA_URL")
  directUrl = env("POSTGRES_URL_NON_POOLING")
}
```

**第2步：修改 .env 文件**

打开 `.env`，把 Vercel Postgres 的连接字符串粘贴进去：

```env
# 从Vercel Postgres详情页复制
POSTGRES_URL="..."
POSTGRES_PRISMA_URL="..."
POSTGRES_URL_NO_SSL="..."
POSTGRES_URL_NON_POOLING="..."
POSTGRES_USER="..."
POSTGRES_HOST="..."
POSTGRES_PASSWORD="..."
POSTGRES_DATABASE="..."

# 旧的SQLite配置可以注释掉
# DATABASE_URL="file:./dev.db"

CRON_API_KEY="你的随机密钥"
```

**第3步：重新生成数据库**

```bash
# 删除旧的迁移记录
rmdir /s /q prisma\migrations

# 生成新的PostgreSQL迁移
node_modules\.bin\prisma migrate dev --name init

# 导入种子数据
node_modules\.bin\prisma db seed

# 补充淘宝价格
node_modules\.bin\tsx prisma/seed-tmall.ts
```

**第4步：提交代码**

```bash
git add .
git commit -m "切换到PostgreSQL数据库"
git push
```

### 2.3 在 Vercel 导入项目

1. Vercel Dashboard 点 `Add New...` → `Project`
2. 找到你的 `pc-builder-pro` 仓库，点 `Import`
3. 配置页面：
   - **Framework Preset**：自动识别为 Next.js，不用改
   - **Root Directory**：保持默认
   - **Build Command**：自动识别，不用改
4. 点 **Environment Variables**，添加：
   - `CRON_API_KEY`：填一个随机字符串（比如 `pc-builder-cron-key-2024`）
5. 点 `Deploy`

等待 2-3 分钟，部署完成后会自动跳转到网站地址（类似 `https://pc-builder-pro.vercel.app`）。

### 2.4 关联数据库到项目

1. Vercel 项目页面 → `Storage` 标签
2. 点 `Connect Database`
3. 选择刚才创建的 `pc-builder-db`
4. 重新部署一次（项目页点 `Redeploy`）

---

## 3. 方案二：Railway 部署（备选）

如果你不想换数据库，Railway 支持持久化存储，SQLite 可以正常使用。但 Railway 免费额度有限（每月 $5 额度）。

### 3.1 创建 Railway 项目

1. 登录 Railway，点 `New Project`
2. 选 `Deploy from GitHub repo`
3. 授权 GitHub，选择 `pc-builder-pro` 仓库
4. Railway 会自动识别为 Node.js 项目

### 3.2 配置环境变量

在 Railway 项目的 `Variables` 标签添加：

```
DATABASE_URL=file:./dev.db
CRON_API_KEY=你的随机密钥
NODE_ENV=production
```

### 3.3 配置启动命令

在 `Settings` → `Service` → `Start Command` 填：

```bash
npx prisma migrate deploy && npx prisma db seed && npm start
```

Build Command 保持默认（`npm run build`）。

### 3.4 添加持久化存储

Railway 的文件系统不是持久化的，重启后数据会丢失。需要添加 Volume：

1. 项目页点 `+ New` → `Volume`
2. 挂载路径填：`/app/prisma`
3. 容量选 1GB（免费额度内）
4. 重新部署

### 3.5 生成域名

1. 项目页 → `Settings` → `Networking`
2. 点 `Generate Domain`，会得到一个 `xxx.up.railway.app` 的地址

---

## 4. 环境变量配置

无论用哪种方案，都需要配置以下环境变量：

| 变量名 | 说明 | 示例 |
|--------|------|------|
| `DATABASE_URL` | 数据库连接（SQLite用） | `file:./dev.db` |
| `POSTGRES_PRISMA_URL` | PostgreSQL连接（Vercel用） | 从Vercel复制 |
| `POSTGRES_URL_NON_POOLING` | PostgreSQL直连（Vercel用） | 从Vercel复制 |
| `CRON_API_KEY` | 定时任务密钥 | 任意随机字符串 |
| `NODE_ENV` | 运行环境 | `production` |

---

## 5. 域名绑定（可选）

### 5.1 Vercel 绑定自定义域名

1. 项目页 → `Settings` → `Domains`
2. 输入你的域名，点 `Add`
3. 按提示去域名服务商（阿里云/腾讯云/Cloudflare）添加 DNS 记录：
   - 类型：`A` 或 `CNAME`
   - 按 Vercel 给出的值填写
4. 等待 DNS 生效（几分钟到几小时），Vercel 会自动配置 HTTPS

### 5.2 免费域名推荐

如果没有域名，可以用：
- **Vercel 自带域名**：`xxx.vercel.app`（免费，够用）
- **eu.org**：免费域名申请（https://nic.eu.org，审核较慢）
- **Freenom**：免费顶级域名（部分地区不可用）

---

## 6. 部署后验证

部署完成后，访问网站地址，逐项检查：

| 页面 | 检查项 |
|------|--------|
| 首页 `/` | 导航栏、Hero、功能卡片、热门配置正常显示 |
| 装机页 `/builder` | 能选硬件、总价计算、兼容性报告正常 |
| 推荐页 `/recommend` | 滑块、用途选择、生成推荐正常 |
| 价格页 `/prices` | 硬件列表、双平台价格、跳转正常 |
| API `/api/hardware` | 返回JSON数据，包含硬件列表 |
| API `/api/recommend` | 返回推荐配置 |

如果页面报错，在 Vercel 项目页 → `Logs` 查看错误日志。

---

## 7. 常见问题

### Q1: 部署后页面白屏怎么办？

A: 检查 Vercel/Railway 的构建日志，看是否有编译错误。常见原因：
- 环境变量没配置
- 数据库连接失败
- 依赖安装失败

### Q2: 数据库数据丢了怎么办？

A: 
- Vercel Postgres：数据持久化，不会丢
- Railway + Volume：确保 Volume 挂载路径正确
- 重新运行种子数据：在部署平台的终端执行 `npx prisma db seed`

### Q3: 怎么更新网站代码？

A: 每次修改代码后：
```bash
git add .
git commit -m "修改说明"
git push
```
Vercel/Railway 会自动检测到推送并重新部署。

### Q4: 免费额度够吗？

A:
- **Vercel 免费版**：每月 100GB 带宽、1000 次构建时长、Serverless 函数 10万次/天，个人项目完全够用
- **Vercel Postgres 免费版**：256MB 存储、每月 100万次查询，够用
- **Railway 免费版**：每月 $5 额度，大约能跑一个小项目 24/7

### Q5: 爬虫在部署后能运行吗？

A: 爬虫脚本 `crawler/jd_crawler.py` 需要在本地运行（需要浏览器）。部署后的网站只负责展示数据。你可以：
1. 在本地电脑定期运行爬虫更新价格
2. 或者用 GitHub Actions 定时运行（进阶）

### Q6: 怎么添加更多硬件数据？

A: 直接修改 `prisma/seed.ts`，添加新的硬件记录，然后运行：
```bash
node_modules\.bin\prisma db seed
```
数据会重新导入。

---

## 快速开始总结

```
1. 注册 GitHub + Vercel 账号
2. 安装 Git，上传代码到 GitHub
3. Vercel 创建 Postgres 数据库
4. 修改项目为 PostgreSQL 配置
5. Vercel 导入项目，配置环境变量
6. 点 Deploy，等待完成
7. 访问 xxx.vercel.app
```

遇到问题先看部署平台的日志，90% 的问题都能从错误日志里找到原因。
